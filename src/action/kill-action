#!/usr/bin/env bash

__process_action() {
    source $(__container_get "action-dir")/arguments-trait "$@"

    echo "Killing queue: $queue_pid" > $enquirer

    # exit the current process (the consumer)
    exit 0
}
